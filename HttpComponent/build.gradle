apply plugin: 'com.android.library'

android {
  namespace 'om.arialyy.aria.http'
  compileSdk 34

  defaultConfig {
    minSdk 21
    targetSdk 28
  }

}

dependencies {
  implementation fileTree(dir: 'libs', include: ['*.jar'])

  implementation project(path: ':PublicComponent')
}

